/** Number of cards on ranking on 1 screen */
export enum maxCafeDisplayCount {
  desktop = 4,
}

/** Pixels how much cards move on clicking arrow */
export enum scrollOffset {
  left = -1300,
  right = 1300,
}

/** Number of cards on ranking */
export enum maxRanking {
  count = 10,
}

export enum space {
  around = 25,
}
