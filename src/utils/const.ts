export const desktop = {
  /** Number of cards on ranking on 1 screen */
  maxCafeDisplayCount: 4,

  /** Pixels how much cards move on clicking arrow */
  scrollOffset: {
    left: -1300,
    right: 1300,
  },

  /** Number of cards on ranking */
  maxRanking: {
    count: 10,
  },

  space: {
    around: 25,
  },
};

export const mobile = {
  /** Number of cards on ranking on 1 screen */
  maxCafeDisplayCount: 4,

  /** Pixels how much cards move on clicking arrow */
  scrollOffset: {
    left: -1300,
    right: 1300,
  },

  /** Number of cards on ranking */
  maxRanking: {
    count: 10,
  },

  space: {
    around: 0,
  },
};
